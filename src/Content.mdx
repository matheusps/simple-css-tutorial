# Tutorial simples de CSS

### por @matheusps

Faaaala meus pupilos! Fim de período, to rushando nas Disciplinas mas não esqueci de vocês.

Muitos vem me perguntando sobre css, principalmente se podem usar libs no projeto... Não vocês não podem!

Mas a melhor notícia é que CSS é a coisa mais fácil do planeta e ao longo desse tutorial simples, vou dar uma série de dicas que vão te ajudar a construir um estilo legal, libs e sem dor de cabeça.

## CSS é interpretado de maneira diferente em cada navegador

CSS não é renderizado igual pelos navegadores! E sim, esse é o principal motivo do seu CSS não parecer "certo". Sempre acaba com uma interface toda quebrada, com espaços brancos e por aí vai. Certo matheus, como eu conserto isso
? - Bom, existem duas abordagems:

- Resetar: Ignora os estilos do browser, zerando tudo! Um exemplo de um reset : https://meyerweb.com/eric/tools/css/reset/
- Normalizar: Adapta os estilos para ficarem mais harmonicos. O normalize é mt usado pra isso: https://necolas.github.io/normalize.css/

⚠️ Vale a pena lembrar que você pode não precisar de um desses, pq naturalmente pode acabar sobrescrevendo estilos!

## Espaçamentos

Imagine dois tipos de espaçamento: interno (padding) e externo (margin).

- `padding`: espaçamento INTERNO em todos os lados (top, right, bottom, left)
- `padding-top`: espaçamento INTERNO para o topo. (análogo para os outros lados)
- `margin`: espaçamento EXTERNO em todos os lados (top, right, bottom, left)
- `margin-top`: espaçamento EXTERNO para o topo. (análogo para os outros lados)
- `...`

Exemplo:

```css
.container {
  padding: 5rem;
  background: #dddddd;
}
.item {
  padding: 2rem;
  margin: 1rem;
  background: #ff851b;
}
```

```html
<div class="container">
  <div class="item"></div>
  <div class="item"></div>
</div>
```

export const DemoSpacing = () => (
  <div style={{ padding: '5rem', background: '#dddddd' }}>
    <div
      style={{ padding: '2rem', margin: '1rem', background: '#ff851b' }}
    ></div>
    <div
      style={{ padding: '2rem', margin: '1rem', background: '#ff851b' }}
    ></div>
  </div>
)

<DemoSpacing />

### Fácil né !?

## Alinhamento

Existem zilhões de formas de alinhar seus templates, eu recomendo `flexbox`.
Por questão de tempo vou deixar um shorhand mt massa pra vcs, que explica bem o que cada regra faz: https://css-tricks.com/snippets/css/a-guide-to-flexbox/.

## Tamanhos

Tu pode usar algumas propriedades para controlar o tamanho de seus elementos:

- `height` : altura
- `width`: largura
- `minHeight`: altura mínima, muito útil se um elemento não puder ser menor que algum valor
- `minWidth`: análogo
- `maxHeight`: altura máxima que um elemento pode ter
- `minWidth`: análogo.

## 💡Protip: Variáveis

Sim! CSS tbm tem variáveis (tá mais pra propriedades)! São uteis para reutilizar valores em diversos lugares, assim tu garante uma consistência bacana no teu app. O projeto da disciplina é muito pequeno, então o escopo não é uma preocupação. Pode definir tudo globalmente através do seletor `:root` como no exemplo abaixo:

```css
:root {
  --fuchsia: #f012be;
  --white: #ffffff;
  --pd: 20px;
}
.bg-fuchsia {
  background-color: var(--fuchsia);
}
.bg-white {
  background-color: var(--white);
}
.fuchsia {
  color: var(--fuchsia);
}
.white: {
  color: var(--white);
}
.container: {
  padding: var(--pd);
}
```

```html
<div class="container bg-fuchsia white">
  <h1>Variáveis funcionam funcionam</h1>
</div>
```

export const DemoVar = () => (
  <div style={{ padding: '20px' }} class="bg-fuchsia white">
    Variáveis funcionam funcionam
  </div>
)

<DemoVar />

## Estilizando Botões e Forms

O projeto pede APENAS isso de estilo praticamente. La vai outra protip: ESTILIZE TODOS OS BOTÕES DE UMA VEZ!. Sim, você pode tratar um grupo de elementos de uma vez com css.

Fica ligado que o botão, assim como inputs, tem evetos: -`:hover`: Quando o mouse fica em cima. -`:focus`: O botão está com o foco em si. -`:active`: O botão está ativo.

```css
root: {
  --c-text: #212121;
  --spacing-v: 6px;
  --spacing-h: 30px;
  --background: #efefef;
  --focus: #0096bfab;
  --button-hover: #ddd;
  --animation-duration: 0.1s;
}

button {
  cursor: pointer;
  color: var(--c-text);
  background-color: var(--background);
  margin-right: var(--spacing-v);
  margin-bottom: var(--spacing-v);
  padding: var(--spacing-v) var(--spacing-h) var(--spacing-v) var(--spacing-h);
  border: none;
  border-radius: (--spacing-v);
  transition: background-color var(--animation-duration) linear, border-color
      var(--animation-duration) linear, color var(--animation-duration) linear, box-shadow
      var(--animation-duration) linear, transform var(--animation-duration) ease;
}

button:hover {
  background: var(--button-hover);
}

button:focus {
  box-shadow: 0 0 0 2px var(--focus);
}

button:active {
  transform: translateY(2px);
}
```

```html
<button>
  Botão legal
</button>
```

export const DemoButton = () => <button>Botão legal</button>

### RESULTADO:

<DemoButton />
